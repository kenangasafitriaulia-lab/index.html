<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Happy Birthday Ma Sayang 🎂</title>

  <style>
    body {
      margin: 0;
      font-family: 'Comic Sans MS', cursive;
      background: linear-gradient(135deg, #fde2e4, #f9c5d1, #fff1f2);
      text-align: center;
      overflow-x: hidden;
    }

    h1 {
      margin-top: 50px;
      font-size: 2.6em;
      color: #ff6f91;
      animation: pop 1.5s infinite;
    }

    p {
      font-size: 1.15em;
      color: #444;
      margin: 12px 25px;
      line-height: 1.6;
    }

    img {
      width: 220px;
      border-radius: 20px;
      margin-top: 20px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.25);
    }

    button {
      margin: 30px 0 40px;
      padding: 15px 28px;
      font-size: 1.1em;
      border: none;
      border-radius: 30px;
      background: #ff8fab;
      color: white;
      cursor: pointer;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }

    button:hover {
      background: #ff6f91;
    }

    /* stickers */
    .stickers {
      margin-top: 15px;
    }

    .stickers img {
      width: 75px;
      margin: 0 10px;
      animation: wiggle 2.5s infinite;
    }

    /* cake & candle */
    .cake-area {
      margin-top: 25px;
      position: relative;
    }

    .cake {
      font-size: 90px;
    }

    .candle {
      font-size: 50px;
      position: absolute;
      top: -25px;
      left: 50%;
      transform: translateX(-50%);
      cursor: pointer;
      animation: flame 1s infinite;
    }

    .hint {
      font-size: 0.9em;
      color: #888;
      margin-top: 10px;
    }

    /* animations */
    .heart {
      position: absolute;
      color: red;
      font-size: 22px;
      animation: float 5s linear infinite;
    }

    @keyframes float {
      from { transform: translateY(100vh); opacity: 1; }
      to { transform: translateY(-10vh); opacity: 0; }
    }

    @keyframes pop {
      0%,100% { transform: scale(1); }
      50% { transform: scale(1.1); }
    }

    @keyframes wiggle {
      0%,100% { transform: rotate(0deg); }
      25% { transform: rotate(5deg); }
      75% { transform: rotate(-5deg); }
    }

    @keyframes flame {
      0%,100% { transform: translateX(-50%) scale(1); }
      50% { transform: translateX(-50%) scale(1.1); }
    }

    @keyframes popSticker {
      0%,100% { transform: scale(1); }
      50% { transform: scale(1.2); }
    }

    @keyframes fadeIn {
      to { opacity: 1; }
    }
  </style>
</head>

<body>

  <!-- 🎶 MUSIC -->
  <audio id="bgm" loop>
    <source src="ive-always-loved-you.mp3" type="audio/mpeg">
  </audio>

  <h1>🎂 Happy Birthday Reihan Adit Tama 🎉</h1>

  <!-- stickers -->
  <div class="stickers">
    <img src="sticker-bear.png" alt="bear">
    <img src="sticker-love.png" alt="love">
    <img src="sticker-cake.png" alt="cake">
  </div>

  <!-- photo -->
  <img src="foto-reihan.jpg" alt="Ma Sayang">

  <!-- cake -->
  <div class="cake-area">
    <div class="cake">🎂</div>
    <div class="candle" id="candle">🕯️</div>
    <p class="hint">Tap the candle, ma sayang 🤍</p>
  </div>

  <!-- text -->
  <p>Hi ma sayang 🤍</p>

  <p>
    Today is your special day, ma sayang,  
    and I just want to say how grateful I am to have ma sayang in my life.  
    Pelan-pelan, kehadiran ma sayang jadi tempat paling nyaman buat aku 🥺
  </p>

  <p>
    Ma sayang makes my ordinary days feel warmer,  
    my bad days feel lighter,  
    and my good days feel even better just by knowing ma sayang is there.
  </p>

  <p>
    Di umur yang baru ini, semoga semua hal baik selalu datang ke hidup ma sayang.  
    Semoga ma sayang selalu sehat, kuat, dan tetap jadi Reihan yang aku kenal —  
    gentle, kind, and deeply special 🤍
  </p>

  <p>
    I may not always say it out loud, ma sayang,  
    but I’m really proud.  
    Proud of who ma sayang are,  
    proud of how far ma sayang have come,  
    and proud of every dream ma sayang is still chasing ✨
  </p>

  <p>
    Thank you for existing, for trying every day,  
    and for being ma sayang.  
    Whenever things feel heavy,  
    I hope ma sayang remember there is always love and support here 🤍
  </p>

  <button onclick="surprise()">Click this, ma sayang 💌</button>

  <p style="margin-top:40px; font-size:1.1em; color:#777; opacity:0; animation: fadeIn 4s forwards;">
    I’ve always loved you, ma sayang.
  </p>

  <script>
    // candle interaction
    const candle = document.getElementById("candle");
    if (candle) {
      candle.addEventListener("click", () => {
        candle.innerHTML = "💨";
        candle.style.animation = "none";

        setTimeout(() => {
          candle.innerHTML = "🤍";
        }, 600);

        setTimeout(() => {
          alert(
            "Make a wish, ma sayang 🤍\n\n" +
            "May this year bring peace, warmth, and happiness.\n" +
            "I’ll always wish the best for ma sayang 🎂"
          );
        }, 900);
      });
    }

    function surprise() {
      const music = document.getElementById("bgm");
      music.volume = 0;
      music.play();

      let vol = 0;
      const fadeIn = setInterval(() => {
        if (vol < 1) {
          vol += 0.02;
          music.volume = vol;
        } else {
          clearInterval(fadeIn);
        }
      }, 150);

      alert(
        "Happy Birthday, ma sayang ❤️\n\n" +
        "I hope this year brings ma sayang closer to every dream.\n" +
        "May happiness and health always stay with ma sayang.\n\n" +
        "Ma sayang means more than words can explain 🤍"
      );

      for (let i = 0; i < 35; i++) {
        let heart = document.createElement("div");
        heart.className = "heart";
        heart.innerHTML = "❤️";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.animationDuration = (Math.random() * 3 + 2) + "s";
        document.body.appendChild(heart);
        setTimeout(() => heart.remove(), 5000);
      }

      // emoji sticker
      let cuteSticker = document.createElement("div");
      cuteSticker.innerHTML = "🎂🤍🥺";
      cuteSticker.style.position = "fixed";
      cuteSticker.style.bottom = "25px";
      cuteSticker.style.right = "25px";
      cuteSticker.style.fontSize = "55px";
      cuteSticker.style.animation = "popSticker 1.5s ease-in-out infinite";
      document.body.appendChild(cuteSticker);

      setTimeout(() => cuteSticker.remove(), 6000);
    }
  </script>

</body>
</html>